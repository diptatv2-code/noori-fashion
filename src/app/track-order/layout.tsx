import type { Metadata } from "next";
export const metadata: Metadata = { title: "Track Order | Noori Fashion" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
