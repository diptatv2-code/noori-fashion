import type { Metadata } from "next";
export const metadata: Metadata = { title: "Checkout | Noori Fashion" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
