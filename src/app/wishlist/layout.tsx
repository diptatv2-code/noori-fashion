import type { Metadata } from "next";
export const metadata: Metadata = { title: "Wishlist | Noori Fashion" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
